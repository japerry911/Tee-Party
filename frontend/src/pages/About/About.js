import React from 'react';
import { useStyles } from './AboutStyles';

const About = () => {
  const classes = useStyles();

  return (
    <div>
      <h1>About Page</h1>
    </div>
  );
};

export default About;
